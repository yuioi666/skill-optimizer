{
  "name": "skill-optimizer-local",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": { "node": ">=22" },
  "scripts": {
    "test": "node --test",
    "validate": "node scripts/cli.mjs validate",
    "doctor": "node scripts/cli.mjs doctor",
    "demo": "node scripts/cli.mjs run --adapter mock",
    "optimize": "node scripts/cli.mjs run --adapter codex",
    "smoke": "node scripts/cli.mjs smoke"
  }
}
